spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    url: jdbc:mysql://localhost:3306/test
    password: '1234'
  jpa:
    hibernate:
      ddl-auto: update
    generate-ddl: true
    show-sql: 'true'
    properties:
      hibernate:
        format_sql: true